define("config", function () {
  return {

    "delving": {
      "uri": "http://onh-prod.delving.org/api"
    },

    "mapbox": {
      "accessToken": "",
      "baseLayerId": ""
    },

    "routeyou_proxy": {
      // URI to proxy for RouteYou API
      "uri": "",
      // Owner of the routes that should be shown in drop down menu.
      "owner_id": 0
    }
  };
});
